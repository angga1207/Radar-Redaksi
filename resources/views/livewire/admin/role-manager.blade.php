<div><div class="flex items-end justify-between gap-4"><div><span class="eyebrow">Keamanan</span><h1 class="mt-1 font-display text-3xl font-bold">Peran & hak akses</h1><p class="mt-2 text-sm text-muted">Atur kemampuan setiap peran redaksi tanpa mengubah kode.</p></div></div>
@if(session('success'))<p class="mt-5 rounded-lg bg-green-100 p-3 text-green-900 dark:bg-green-950 dark:text-green-200">{{ session('success') }}</p>@endif
<div class="mt-6 grid gap-5 lg:grid-cols-[320px_1fr]"><section class="admin-card"><h2 class="font-bold">Daftar peran</h2><div class="mt-4 grid gap-2">@foreach($roles as $role)<button type="button" wire:click="edit({{ $role->id }})" class="flex min-h-12 items-center justify-between rounded-lg border border-line px-3 text-left hover:border-brand"><span><strong class="block">{{ $role->label }}</strong><small class="text-muted">{{ $role->name }}</small></span><span class="status-pill">{{ in_array('*', $role->permissions, true) ? 'Semua' : count($role->permissions) }}</span></button>@endforeach</div></section>
<section class="admin-card">@if($editingId)<form wire:submit="save"><label class="label" for="role-label">Nama tampilan</label><input id="role-label" wire:model="label" class="field"><fieldset class="mt-5"><legend class="font-bold">Kemampuan</legend><div class="mt-3 grid gap-2 sm:grid-cols-2">@foreach($availablePermissions as $value => $permissionLabel)<label class="flex min-h-11 items-center gap-3 rounded-lg border border-line px-3"><input type="checkbox" wire:model="permissions" value="{{ $value }}"> <span class="text-sm">{{ $permissionLabel }}</span></label>@endforeach</div></fieldset><button class="btn-primary mt-5">Simpan hak akses</button></form>@else<div class="grid min-h-72 place-items-center text-center text-muted"><div><i class="fa-solid fa-user-shield text-3xl text-brand"></i><p class="mt-3">Pilih peran untuk melihat hak aksesnya.</p></div></div>@endif</section></div></div>
