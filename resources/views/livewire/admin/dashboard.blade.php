<div><div class="flex flex-wrap items-end justify-between gap-4"><div><span class="eyebrow">Meja redaksi</span><h1 class="mt-1 font-display text-3xl font-bold">Ringkasan hari ini</h1></div><a class="btn-primary" href="{{ route('admin.articles.create') }}" wire:navigate>Tulis berita</a></div><div class="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">@foreach([['Draft',$counts['draft'],'fa-file-pen'],['Menunggu review',$counts['review'],'fa-clock'],['Terbit hari ini',$counts['publishedToday'],'fa-circle-check'],['Total pembaca',number_format($counts['totalViews']),'fa-users']] as $stat)<div class="admin-card"><i class="fa-solid {{ $stat[2] }} text-brand"></i><p class="mt-5 text-sm text-muted">{{ $stat[0] }}</p><p class="mt-1 text-3xl font-bold">{{ $stat[1] }}</p></div>@endforeach</div><section class="admin-card mt-7"><div class="section-heading"><div><span class="eyebrow">7 hari terakhir</span><h2>Tren pembaca</h2></div></div><div class="mt-6 grid h-52 grid-cols-7 items-end gap-2" role="img" aria-label="Grafik pembaca tujuh hari terakhir">@foreach($viewTrend as $point)<div class="grid h-full grid-rows-[1fr_auto] gap-2 text-center"><progress class="view-trend-bar" max="{{ $maxTrendViews }}" value="{{ $point['count'] }}" aria-label="{{ $point['date'] }}: {{ $point['count'] }} pembaca"></progress><div><strong class="block text-xs">{{ number_format($point['count']) }}</strong><span class="text-xs text-muted">{{ $point['label'] }}</span></div></div>@endforeach</div></section><div class="mt-7 grid gap-6 xl:grid-cols-[1.5fr_1fr]"><section class="admin-card"><div class="section-heading"><h2>Aktivitas terbaru</h2><a href="{{ route('admin.articles.index') }}">Kelola</a></div><div class="mt-4 overflow-x-auto"><table class="data-table"><thead><tr><th>Judul</th><th>Status</th><th>Penulis</th></tr></thead><tbody>@forelse($recent as $item)<tr><td><a class="font-semibold hover:text-brand" href="{{ route('admin.articles.edit',$item) }}">{{ $item->title }}</a><p class="text-xs text-muted">{{ $item->category->name }}</p></td><td><span class="status-pill">{{ $item->status->label() }}</span></td><td>{{ $item->author->name }}</td></tr>@empty<tr><td colspan="3" class="text-center text-muted">Belum ada aktivitas berita.</td></tr>@endforelse</tbody></table></div></section><section class="admin-card"><h2 class="text-xl font-bold">Paling banyak dibaca</h2><ol class="mt-4 divide-y divide-line">@forelse($popular as $item)<li class="flex gap-3 py-3"><span class="text-2xl font-bold text-brand">{{ $loop->iteration }}</span><div><p class="font-semibold leading-tight">{{ $item->title }}</p><p class="text-xs text-muted">{{ number_format($item->views_count) }} pembaca</p></div></li>@empty<li class="py-6 text-sm text-muted">Belum ada data pembaca.</li>@endforelse</ol></section></div></div>
