<div><span class="eyebrow">Keamanan</span><h1 class="mt-1 font-display text-3xl font-bold">Audit log</h1><div class="admin-card mt-6 overflow-x-auto"><table class="data-table"><thead><tr><th>Waktu</th><th>Aktor</th><th>Aktivitas</th><th>Subjek</th><th>IP</th></tr></thead><tbody>@forelse($logs as $log)<tr wire:key="log-{{ $log->id }}"><td>{{ $log->created_at->format('d/m/Y H:i') }}</td><td>{{ $log->actor?->name??'Sistem' }}</td><td><span class="status-pill">{{ $log->event }}</span></td><td>{{ class_basename($log->subject_type) }} #{{ $log->subject_id }}</td><td>{{ $log->ip_address }}</td></tr>@empty<tr><td colspan="5" class="py-10 text-center text-muted">Belum ada aktivitas tercatat.</td></tr>@endforelse</tbody></table><div class="mt-5">{{ $logs->links() }}</div></div></div>
