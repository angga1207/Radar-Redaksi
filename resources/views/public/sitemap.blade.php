{!! '<'.'?xml version="1.0" encoding="UTF-8"?'.'>' !!}<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"><url><loc>{{ route('home') }}</loc></url>@foreach($categories as $item)<url><loc>{{ route('categories.show',$item) }}</loc><lastmod>{{ $item->updated_at->toAtomString() }}</lastmod></url>@endforeach @foreach($articles as $item)<url><loc>{{ route('articles.show',$item) }}</loc><lastmod>{{ $item->updated_at->toAtomString() }}</lastmod></url>@endforeach @foreach($pages as $item)<url><loc>{{ route('pages.show',$item) }}</loc><lastmod>{{ $item->updated_at->toAtomString() }}</lastmod></url>@endforeach</urlset>
