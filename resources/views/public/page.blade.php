@extends('layouts.public') @section('title', ($page->seo_title ?: $page->title).' — Radar Redaksi') @section('meta_description', $page->seo_description ?: str($page->body)->stripTags()->limit(155)) @section('content')<article class="site-container py-12"><div class="mx-auto max-w-3xl"><span class="eyebrow">Informasi</span><h1 class="mt-3 font-display text-5xl font-bold">{{ $page->title }}</h1><div class="article-body mt-8">{!! $page->body !!}</div></div></article>@endsection
