@extends('layouts.public') @section('content')<div class="site-container py-10"><header class="max-w-3xl">@isset($author)<div class="flex items-center gap-5">@if($author->avatar)<img src="{{ $author->avatar }}" alt="Foto {{ $author->name }}" class="size-24 rounded-full border-4 border-surface object-cover shadow-lg">@else<div class="grid size-24 place-items-center rounded-full bg-brand text-3xl font-bold text-white">{{ str($author->name)->substr(0, 1)->upper() }}</div>@endif<div>@endisset<span class="eyebrow">{{ isset($author) ? 'Profil penulis' : 'Kanal Radar' }}</span><h1 class="mt-2 font-display text-4xl font-bold">{{ $title }}</h1><p class="mt-3 text-muted">{{ $description }}</p>@isset($author)</div></div>@endisset</header><div class="mt-8 grid gap-7 sm:grid-cols-2 lg:grid-cols-3">@forelse($articles as $article)<x-article-card :article="$article" />@empty<p class="col-span-full rounded-xl border border-line bg-surface p-8 text-center text-muted">Belum ada berita di kanal ini.</p>@endforelse</div><div class="mt-10">{{ $articles->links() }}</div></div>@endsection
