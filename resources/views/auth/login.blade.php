@extends('layouts.public') @section('content')<div class="site-container flex min-h-[65vh] items-center justify-center py-12"><div class="w-full max-w-md rounded-2xl border border-line bg-surface p-8"><span class="eyebrow">Akses internal</span><h1 class="mt-2 font-display text-3xl font-bold">Masuk ke ruang redaksi</h1><form method="POST" action="{{ route('login.store') }}" class="mt-7 grid gap-5">@csrf<div><label for="email" class="label">Email</label><input id="email" type="email" name="email" value="{{ old('email') }}" class="field" required autofocus>@error('email')<p class="field-error">{{ $message }}</p>@enderror</div><div><label for="password" class="label">Kata sandi</label><input id="password" type="password" name="password" class="field" required></div><label class="flex items-center gap-2 text-sm"><input type="checkbox" name="remember" value="1"> Ingat saya</label><button class="btn-primary w-full">Masuk</button></form></div></div>@endsection
